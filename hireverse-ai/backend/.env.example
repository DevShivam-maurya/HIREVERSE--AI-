PORT=5000
JWT_SECRET=hireverse_super_secret_change_me
JWT_EXPIRES_IN=7d
# Optional: add a Gemini API key to enable real AI-generated interview questions
# and resume feedback. Without it, the app runs on a smart rule-based engine
# so everything still works end-to-end out of the box.
GEMINI_API_KEY=your_api_key_here
